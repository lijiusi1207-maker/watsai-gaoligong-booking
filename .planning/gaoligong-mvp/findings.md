# 现状发现

- 仓库：`booking-miniprogram`，本地分支 `main`，提交 `73f160b`。
- 工作区已有未跟踪 `HANDOFF.md`，不得覆盖或删除。
- 当前后端为单文件零依赖 Node 服务，默认端口 8090，JSON 文件持久化。
- 当前固定管理 token 为 `admin888`，用户密码明文写入 `data.json`。
- 已有 `__verify.js` 与 `__verify2.js`，分别覆盖基础链路和富字段活动/优胜标记。
- 文档审计明确的 P0：安全、真凭证、预约完整性、家庭多人预约、跨设备同步、通知和活动详情。
- 外部阻塞：GitHub 凭证、Render/VPS 账号、微信/短信资质；本地可以完成代码和验证准备。

